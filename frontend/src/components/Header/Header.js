import React from "react";
import PropTypes from "prop-types";
import styles from "./styles.module.css";

const Header = () => <header className={`${styles.header} shadow`}></header>;

export default Header;
